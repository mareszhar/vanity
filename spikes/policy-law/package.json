{
  "name": "spike-policy-law",
  "type": "module",
  "private": true,
  "description": "Isolation spike for policy-projected constructors and enforcement reach.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@mszr/selenita": "^0.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
