{
  "name": "spike-authoring-grammar",
  "type": "module",
  "private": true,
  "description": "Isolation spike for Vanity's symmetric direct/callback/module/array authoring grammar.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@mszr/selenita": "^0.2.2",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
