{
  "name": "spike-system-scale",
  "type": "module",
  "private": true,
  "description": "Agnostic TS spike: a multi-facet accumulating builder (groups + branched fields + variants + contributions + helpers) at REALISTIC whole-system scale. Proves the lean-accumulation discipline holds when links are few but payloads are wide.",
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "generate": "node scale/generate.mjs",
    "scale": "node scale/measure.mjs"
  },
  "devDependencies": {
    "@mszr/selenita": "^0.2.2",
    "type-fest": "^5.8.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
