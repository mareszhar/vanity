{
  "name": "spike-plugin-module-ownership",
  "type": "module",
  "private": true,
  "description": "Agnostic Vite spike: module ownership, hook cost, dependency optimization, SSR externalization, and HMR across installed packages.",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "check": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "validate": "pnpm run check && pnpm run test"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^6.0.3",
    "vite": "8.2.2",
    "vitest": "4.1.10"
  }
}
